---
notion_page_id: 36181856-910c-817b-9823-f78d5b676692
---


# 테이블만
select table_name from user_tables order by table_name; 

# 테이블, 뷰, 시퀀스
select table_name from tabs;

# 테이블 컬럼 조회
SELECT column_name, data_type, data_length
FROM user_tab_columns
WHERE table_name = 'T_USER';

# 내 스키마 테이블 목록 조회
SELECT
    table_name,
    column_name,
    data_type,
    data_length
FROM user_tab_columns
ORDER BY table_name, column_id;
  
-------------------------------------------------------------------------------------

# 컬럼 조회
SELECT column_name
FROM user_tab_columns
WHERE table_name = 'EMP';



# 오라클 컬럼 타입 조회
SELECT COLUMN_NAME,
       DATA_TYPE,
       DATA_LENGTH,
       DATA_PRECISION,
       DATA_SCALE
FROM ALL_TAB_COLUMNS
WHERE OWNER = 'VARDNA'
  AND TABLE_NAME = 'T_CUE'
  AND COLUMN_NAME = 'F_DATE';
  
-------------------------------------------------------------------------------------

# 코멘트 조회
select comments from user_tab_comments;

# DDL
SELECT DBMS_METADATA.GET_DDL('TABLE', 'EMP')
FROM dual;